import { Router } from 'express';
const router = Router();

// create
router.post('/', (req, res, next) => {

})

// read
router.get('/', (req, res, next) => {

})

// update
router.put('/:pilotID', (req, res, next) => {

})

// delete
router.delete('/:pilotID', (req, res, next) => {
  
})

export default router;
